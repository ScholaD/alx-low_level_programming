#include "main.h"
/**
 * _abs - Entry point
 *@num:character in ASCII
 * Return: Return num for greater than -1 for less  than
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}

